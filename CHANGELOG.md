# CHANGELOG

## v7.2 — 2026-08（中国名人三柱批量研究）

- 新增 `data/zh_birthdates.json`：中文维基 33 个职业分类采集，
  2626 位中国名人的完整公历出生日期（超 1000 目标）。
- 新增 `scripts/zh1000.py`：批量三柱排盘与分布统计（复现脚本）。
- 新增 `references/chinese-1000.md`：分布结果与诚实解读——日主分布不显著
  偏离均匀；月令秋冬季偏多与出生季节模式一致；无时辰样本不做命运拟合。
- 网络资料核验：维基「占卜」条目引 1958 年伦敦 2000+ 同时辰出生追踪
  （性格职业不相似）、700+ 占卜师预测不优于随机；「算命」条目归因巴纳姆效应。
- docs/usage.md 增加数据集使用纪律章节。

## v7.1 — 2026-08（同盘异命分析与工具化）

在 v7 工程修正之上，用 20 位中外名人做了第二轮拟合验证
（详见 `references/famous20-validation.md`），并把发现落成模型纪律：

- **新增 `scripts/compare_charts.py`**：两盘对比工具，输出四柱、真太阳时、
  交运日差、大运表、命宫、胎元两派与 ±3h 时辰敏感性；同盘分析前必跑。
- **同盘异命写入纪律**（calibration.md 第 5 节）：三书原典依据
  （三命通会时地分野、滴天髓生时章、神峰通考山川风气）；
  区分维度：时之先后 / 山川之异 / 世德之殊 / 昼夜阴晴 / 时辰可信度。
- **占位修正**：林肯/达尔文"同盘"源于两人时辰均无记载、以正午占位；
  真实时辰若不同则四柱即不同。引用该反例须标注"时辰占位，同盘存疑"。
- **guardrail 新增**："八字信息不完备"——时辰占位的盘不得进入结论。
- 测试 56 → 58 条（同盘对比 + 时辰敏感性）。

## v7 — 2026-08（排盘引擎历法与天文修正）

模型层未动（仍是 v6 的八书体系、M0—M6）。本版全部改动集中在**排盘引擎的正确性**。

起因是一轮跨年代交叉验证：50 位古今中外人物（20 人首轮 ＋ 30 人留出集）＋ 16 个边界
采样点，与两个独立第三方实现比对 —— `sxtwl`（寿星天文历 C++ 绑定，只用来核日柱）
与 `lunar_python`（lunar-javascript 移植，核年/月/时柱）。

### 修正（会改变输出的 bug）

| # | 问题 | 影响 |
|---|---|---|
| 1 | **ΔT 只有以 1900 为基准的一段**，往前外推发散 | 1500 年算出 −63 天、1800 年 −7.4 小时。这是下面 2 号的根因 |
| 2 | **节气求根不收敛却静默返回初值** | `t0±2` 天二分、最多扩到 ±10 天；1680 年前目标落在窗外时 `f(lo)*f(hi)` 恒同号，100 次二分白跑，`return (lo+hi)/2` 把初值当结果。铁证：1500 与 1582 的立春都是 18:29:00、寒露都是 06:20:56，秒级完全相同。明清及更早的盘月柱整月错位 |
| 3 | **中国夏令时表被无条件套到全世界** | `dst_offset_hours` 只看日期，不看时区。1988 年夏天出生的东京/纽约/新德里盘也一律回拨 1 小时，真太阳时恒错 60 分钟 |
| 4 | **输入侧与输出侧历法不一致** | `jd_from_gregorian` 用格里历，而 `jd_to_datetime` 在 1582 前走儒略历分支 —— 节气时刻与出生时刻是拿两套历法的日期在比较；还会造出 1000-02-29（儒略闰、格里不闰）让 `datetime` 直接抛异常 |
| 5 | **胎元只给"300 日法"且冠《三命通会》之名** | 与通行口诀（月干进一位、月支进三位）不符，同一盘可差一整柱（月柱己巳 → 进三位庚申 / 300 日法己未）；且倒推时把出生时刻截断成 00:00，胎元落在交节当天会错一整月 |

### 新增

- **`--calendar auto|julian|gregorian`**：输入日期的历法声明。`auto` 在 1582-10-15
  之前按儒略历解释（史料惯例，与 sxtwl / lunar-javascript 一致）。
  ⚠️ 历法基准要**逐条确认**：同一批史料里可能混着已经换算过的格里历值，
  对这类日期 `auto` 会多换算一次。
- **胎元两派并列**：主派进三位法 ＋ 旁证 300 日法，并给 `agree` 标记。
  与换日流派同样的处理方式（门派分歧并列，不作独断）。
- **`term_ut_jd` 加 `lru_cache`**：单次 `calc` 原本要调 20027 次 `solar_longitude`；
  测试套件从 4.1s 降到 0.07s。
- **测试 45 → 56 条**，新增覆盖：ΔT 古代参考值与分段连续性、节气求解跨千年收敛、
  同一节气在不同年份时刻不得相同、中国夏令时只对东八区生效、`--dst on` 覆盖仍有效、
  儒略历输入换算、`--calendar gregorian` 不换算、公元 800—1265 年不崩。

### 验证结果

| 检验 | v6 | v7 |
|---|---|---|
| 30 人留出集（未参与修复）年/月柱与参照一致 | 错 6 | **0** |
| 20 人 ＋ 16 边界点，共 36 例 | 错 5 | 只剩 1 例（换日流派差异，设计使然） |
| 1500 年 ΔT | −63 天 | +198 秒 |
| 1582 前日柱与两参照 | 差 9—10 天 | 完全对齐 |
| 公元 1000 年前排盘 | 抛异常 | 正常 |
| 测试套件 | 45 条 / 4.1s | 56 条 / 0.07s |

### 附带发现（不改代码，记录备查）

- **拿知名历史人物做命理回测在方法上不成立**。做了一次 2×2 强制配对盲测（25 题，
  随机基线 50%），结果 25/25＝100%、p<0.0001，但 25 题里有 24 题判读方主动声明
  "我认出了人物是谁"——年柱干支可直接换算公历年、月柱地支就是出生月份。
  剔除泄露题后剩 1 题，无可用样本。
- 那唯一没泄露的一题恰是**林肯与达尔文同日生**（1809-02-12）：两张盘的四柱、五行、
  神煞、胎元、命宫、大运完全一致，而两人命运截然不同。
- **Wikidata 的历法标注不可信**：达芬奇/哥白尼/米开朗基罗/伽利略等条目存的是格里历
  数值却把 `timeCalendarModel` 标成儒略历。核 1582 年前的日期不能信它的历法字段。

## v6 — 2026-08（第二轮古籍贯通）

模型层从"三书体系"升级为"八书体系"：精读新增《三命通会》《渊海子平》《神峰通考》
《协纪辨方书》《果老星宗》，回头复核《千里命稿》《子平真诠》《滴天髓》。
逐书解读、现代分层与不采用清单见 [classics-v6.md](references/classics-v6.md)。

### 模型层增量

- **M0 锚点与历法纪律**（新增）：锚点时间可复核性优先；择吉神煞只作文化注记；
  胎元（300 日法）、命宫（三命通会法）随排盘输出，低置信旁证。
- **M3 病药八法**：病药三分升级为"雕枯旺弱 × 损益生长"矩阵（神峰通考）。
- **M5 岁运四分类**：战/冲/和/好操作化（滴天髓岁运章）；太岁纪律
  （岁运并临、伏吟返吟、征太岁、晦气）三书互证后入规。
- **M6 断语三级制**：S 结构化规则 / C 文化注记 / X 不采用；多书互证要求。
- **六亲**：明干取/暗中求、喜神看财官（滴天髓）、旧书性别断语中性化。
- **神煞纪律强化**：《神峰通考》谬说批判 ＋《子平真诠》"星辰无关格局"
  成为 guardrail 的古典依据；日贵/魁罡/趋艮趋乾等"飘空"格名列入不采用清单。

### 工程层

- `pai_pan.py`：新增胎元、命宫输出；`version` 升为 v6。
- `tests/`：新增胎元 300 日法、命宫三命通会例（甲子年三月戌时→丁卯）、鸣潮命宫
  三条测试，全套 45 条通过。

## v5.2 — 2026-08

- **skill 正式命名为 `BFFT`**：SKILL.md frontmatter、使用文档、agents 配置、
  脚本头部统一改名；仓库路径与 GitHub 链接保持不变（`bazi-entity-v3`）。
- **分发包更新为 `dist/BFFT.zip`**：压缩包根目录为 `BFFT/`，使用文档随包分发
  （`docs/usage.md`，另在根目录附 `使用文档.md` 同内容副本）。
- 删除旧的 `dist/bazi-entity-v5.zip`。

## v5 — 2026-08

模型层与 v4 相同（三书体系、六模块、层次原则未变）。本版全部改动集中在**排盘引擎的正确性**
与**结论的可证伪性**两件事上。

### 修正（会改变输出的 bug）

| # | 问题 | 影响 |
|---|---|---|
| 1 | **流年干支未按立春分界** | 年柱按立春换、流年却按公历年换，年初一个多月的流年干支错一位。M5 时序引擎的岁运并看因此可能整条读错 |
| 2 | `add_months` 里 `min(day, 28)` | 起运与交运日期最多偏差 3 天 |
| 3 | 节气时刻 `int()` 截断且丢秒 | 交节时刻最多少 1 分钟，边界盘可能错月柱 |
| 4 | 缺 ΔT (TT−UT) 修正 | 黄经公式吃 TT 却当 UT 用，节气时刻约偏 1 分钟 |
| 5 | 神煞段内空循环死代码 | 无功能影响，已删 |
| 6 | **`calibration.md` 大运序列漏排「辛未」** | 己巳→庚午→~~壬申~~ 应为 己巳→庚午→**辛未**→壬申→癸酉。此后各步整体提前十年，凡依赖"2038 起为壬申运"的旧结论需顺延十年 |

### 新增

- **真太阳时**：`--lon` 经度修正 ＋ 均时差。此前同一时区内所有经度共用一套时柱；
  实测同一钟表时刻 07:30，乌鲁木齐(87.6°E) 真太阳时 04:20、上海(121.5°E) 06:35，
  时柱一个庚寅一个辛卯——原版对这两地给出同一时柱，其中必有一错。人盘不给经度会告警。
- **中国夏令时 1986—1991**：自动识别并回拨 1 小时，可用 `--dst off` 覆盖。
- **换日流派可切换**：`--day-boundary zi|midnight`，并在输出里并列另一派日柱。
  此前换日规则硬编码单派，与"门派分歧并列、不作独断"的 guardrail 自相矛盾。
- **边界告警**：距节气不足 1 小时时提示需外部核对。
- **`tests/`**：42 条不变量测试。核心一条是拿算出的节气时刻回代黄经公式验证是否命中目标黄经，
  不依赖任何外部数据即可抓出求根、ΔT、时区换算的错误。另有月柱进位、日柱递增、
  立春年柱切换、大运连续性（就是能抓出第 6 条那种漏排的检查）、真太阳时对称性等。
  绝对精度校验留 `tests/fixtures.csv` 供填权威万年历样本——不预置"凭记忆写的标准答案"，
  那比没有测试更糟。**修正一个模板与测试互相打架的 bug**：注释行在表头之前时，
  `DictReader` 会把首行注释当成 fieldnames、永远读不到数据，`_rows()` 现已先滤注释再解析。
- **`predictions/` 预测登记**：每条判据独立 commit，**git 首次提交时间即事前证明**。
  `scripts/verify.py` 自动比对提交时间与窗口起点，晚于窗口起点的不计 A 级。
- **`scripts/verify.py`**：命中率、Brier 分数、**技巧分数**（相对基线）。
- **基线对照（base_rate）成为必填**：判据必须写明"不用本模型、仅凭行业常识的发生概率"。
  例如"运营中的手游年内有一次重磅企划官宣"基线接近 100%，命中也无信息量，
  这类条目标 `low_information` 并从命中统计中剔除。技巧分数 ≤ 0 即视为模型未提供信息。

### 结构

- 删除 `docs/bazi-entity-v4-模型说明.md`（与 `references/v4-spec.md` MD5 完全相同的重复文件）
- `references/v4-spec.md` → `references/model-v5.md`
- `references/model-v3.md` → `references/model-v3-deprecated.md`（加 DEPRECATED 头）
- 中文文件名改英文（`docs/usage.md`），避免 raw URL 需要 percent-encode
- SKILL.md 的 `description` 从 100+ 词压到 55 词——它常驻上下文，能力清单挪进正文按需加载
- 加 GitHub Actions：跑测试 ＋ `verify.py --strict`

### 已知边界

- 太阳黄经用 Meeus 低精度式，节气时刻精度约分钟级；距节气 1 小时内的盘请与权威万年历核对。
- 夏令时日期表依据公开资料，与出生地实际档案可能有差异。
- **绝对精度首批已对齐**：`tests/fixtures.csv` 现含 13 条带来源的权威万年历样本
  （节气交界前后、1988 夏令时、23:30 换日两派、乌鲁木齐/哈尔滨极端经度、闰年），
  测试全绿。节气本身仍为分钟级精度，距节气 1 小时内的盘请继续人工核对。

## v4

三书融合（《千里命稿》《子平真诠》《滴天髓》＋梁湘潤今註），六模块管线，六亲双轨。

## v3

万物命理统一模型：human / game / product / company 四类实体与寿命缩放。
